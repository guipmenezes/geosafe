<div class="flex w-screen-default h-screen flex-col w-full">
  <!-- Primeira div: Barra superior com logo e informações -->
  <div class="flex flex-col sm:flex-row justify-between items-center w-full px-4 sm:px-5 py-4 sm:py-5 space-y-4 sm:space-y-0">
    <div class="flex flex-col sm:flex-row items-center space-y-4 sm:space-y-0">
      <%= render(Logo::LogoComponent.new) %>
      <div class="flex flex-col sm:flex-row items-center text-primary500 space-y-2 sm:space-y-0 sm:space-x-5 sm:ml-10 mt-4 sm:mt-0">
        <h3 class="font-semibold">Funcionalidades</h3>
        <h3 class="font-semibold">Por que contribuir</h3>
        <h3 class="font-semibold">Planos</h3>
      </div>
    </div>
    <div class="flex items-center space-x-5">
      <button class="bg-primary400 text-white font-semibold w-24 sm:w-20 py-2 sm:py-1 rounded-lg">Entre</button>
      <button class="text-primary500 font-semibold">Cadastre-se</button>
    </div>
  </div>

  <!-- Banner CTA com imagem e cantos arredondados -->
  <div class="relative w-full h-[90vh] sm:h-screen overflow-hidden rounded-2xl sm:rounded-[2rem]">
    <!-- Imagem de fundo -->
    <%= image_tag "mika-baumeister-nDciGidCdQo-unsplash.jpg", alt: "Imagem de homem segurando um celular com o mapa aberto.", class: "absolute inset-0 w-full h-full object-cover" %>
    
    <!-- Gradiente de sobreposição -->
    <div class="absolute inset-0 bg-gradient-to-r from-primary900/90 via-primary700/80 to-transparent"></div>
    
    <!-- Conteúdo -->
    <div class="relative z-10 h-full flex items-center">
      <div class="text-white p-6 sm:p-10 max-w-xl sm:max-w-2xl">
        <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold mb-4 sm:mb-6">
          Tenha consciência do que acontece ao seu redor
        </h1>
        <button class="bg-primary400 text-white flex justify-center items-center font-semibold text-sm sm:text-base py-2 sm:py-3 px-4 sm:px-6 rounded-lg hover:bg-[#4A7DAB] transition duration-300">
          CADASTRE-SE NA GEOSAFE
        </button>
      </div>
    </div>
  </div>

  <div class="w-full px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto py-12">
    <div class="max-w-4xl mx-auto">
      <p class="text-center text-lg sm:text-lg">
        <span class="font-bold text-primary300">Geosafe</span> tem o objetivo de fornecer para você, por meio da <span class="font-bold text-secondary300">localização geográfica</span>, uma noção do que acontece nos diferentes
        locais em Brasília (por enquanto), para que os moradores <span class="font-bold text-secondary300">tenham noção</span> do que está acontecendo onde moram e também para os 
        locais que deseja visitar.
      </p>
    </div>
  </div>

  <div class="border-b border-t">
    <%= render "homepage/partials/features" %>
  </div>
</div>
